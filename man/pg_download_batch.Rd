% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data.R
\name{pg_download_batch}
\alias{pg_download_batch}
\title{Download one batch of files}
\usage{
pg_download_batch(urls, destfiles, progress = TRUE)
}
\arguments{
\item{urls}{Character vector of urls to download.}

\item{destfiles}{Character vector of final file paths.}

\item{progress}{Logical. Whether to show a progress bar.}
}
\value{
data.frame with one row per requested file.
}
\description{
Writes to \verb{<destfile>.part} so that an interrupted transfer never leaves a
truncated file under the name callers treat as complete, and so that a rerun
can resume it.
}
\details{
Note that \code{\link[curl:multi_download]{curl::multi_download()}} reports the \emph{effective} url, i.e. the one
reached after redirects, so the request url cannot be used to match results
back to requests. \code{destfile} comes back exactly as passed and is used instead.

\code{\link[curl:multi_download]{curl::multi_download()}} transfers everything it is given in parallel and
exposes no connection limit, so the number of urls passed in is what bounds
concurrency. Callers keep that number small rather than this function.
}
\keyword{internal}
