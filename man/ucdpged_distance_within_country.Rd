% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_ucdp.R
\name{ucdpged_distance_within_country}
\alias{ucdpged_distance_within_country}
\title{Distance to nearest UCDP GED event within each country}
\usage{
ucdpged_distance_within_country(
  measurement_date,
  ged = read_ucdp_ged(),
  cshp = read_cshapes(),
  config = pg_current_config(),
  geodesic = NULL
)
}
\arguments{
\item{measurement_date}{A single \code{Date} object specifying the measurement date.}

\item{ged}{An \code{sf} object containing UCDP GED data. Defaults to
\code{\link{read_ucdp_ged}()}.}

\item{cshp}{An \code{sf} object containing CShapes 2.0 boundary data. Defaults to
\code{\link{read_cshapes}()}.}

\item{config}{A \code{pg_config} object. Defaults to \code{\link{pg_current_config}()}.}

\item{geodesic}{Logical or NULL. If TRUE, computes distances in WGS84 using
spherical (S2) geometry and reprojects the result to the config CRS. If
FALSE, uses Euclidean distances in the config CRS. Default NULL
auto-detects: geodesic for projected CRS (e.g. UTM), native for geographic
CRS (e.g. WGS84, which terra already handles geodesically).}
}
\value{
A \code{SpatRaster} with distances in meters to the nearest conflict event
within the same country. NA indicates no events recorded in that country for
the period.
}
\description{
For each PRIO-GRID cell, computes the distance (in meters) to the nearest
UCDP GED conflict event that occurred within the same country during the
period containing \code{measurement_date}. Cells in countries with no
recorded events are set to NA (not 0), to distinguish absence of events
from proximity to events. Events with spatial precision code >= 6
(national-level or coarser) are excluded.
}
\seealso{
\code{\link{ucdp_ged}} for fatality-count rasters,
\code{\link{read_ucdp_ged}} for loading raw UCDP GED data
}
