% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_data.R
\name{pg_resolve_filenames}
\alias{pg_resolve_filenames}
\title{Resolve the real filenames behind a source's download urls}
\usage{
pg_resolve_filenames(id, write = FALSE, delay = 1)
}
\arguments{
\item{id}{Character. Source id (UUID), as in \link{pgsources}.}

\item{write}{Logical. If TRUE, rewrite the url list file under \code{inst/extdata}.
Defaults to FALSE so that the proposal can be inspected first.}

\item{delay}{Numeric. Seconds to wait between requests. Default 1.}
}
\value{
data.frame of urls, current filenames and resolved filenames (invisibly).
}
\description{
Some sources serve files from urls that do not name them, such as
\verb{https://ndownloader.figshare.com/files/17626052}. Left alone, those land on
disk as \code{17626052}. This developer-facing helper asks each server what the
file is actually called and records the answer as a tab-separated second
column in the source's url list, so that \code{\link[=pg_rawfiles]{pg_rawfiles()}} keeps deriving local
paths offline and the bundled \link{pgchecksum} stays valid.
}
\details{
The name is taken from the \code{Content-Disposition} header when the server sends
one, then from the url the request ended up at after redirects, then from the
stated url. None of these is reliable for every provider: sources behind a
login redirect to the login page, and GitHub redirects to storage urls with
opaque object keys. Read the proposed table before writing, which is why this
is run by a maintainer and committed rather than run by users.
}
\examples{
\dontrun{
priogrid:::pg_resolve_filenames("d99fbea7-2a01-4221-b900-29a58d33f591")
priogrid:::pg_resolve_filenames("d99fbea7-2a01-4221-b900-29a58d33f591", write = TRUE)
}
}
\keyword{internal}
