% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{pg_config}
\alias{pg_config}
\title{Create a PRIO-GRID configuration object}
\usage{
pg_config(
  nrow = 360L,
  ncol = 720L,
  crs = "epsg:4326",
  extent = c(xmin = -180, xmax = 180, ymin = -90, ymax = 90),
  temporal_resolution = "1 year",
  start_date = as.Date("1850-12-31"),
  end_date = Sys.Date(),
  verbose = TRUE,
  automatic_download = TRUE
)
}
\arguments{
\item{nrow}{Integer. Number of rows in the grid.}

\item{ncol}{Integer. Number of columns in the grid.}

\item{crs}{Character. Coordinate reference system string.}

\item{extent}{Numeric vector. Spatial extent c(xmin, xmax, ymin, ymax).}

\item{temporal_resolution}{Character. Increment of temporal sequence, see \link[base:seq.Date]{base::seq.Date}.}

\item{start_date}{Date. The start date for temporal coverage. The month and day define
the measurement date within the temporal resolution.}

\item{end_date}{Date or "today". The end date for temporal coverage.}

\item{verbose}{Logical. If TRUE, print informative messages.}

\item{automatic_download}{Logical. If TRUE, automatically download needed files.}
}
\value{
A \code{pg_config} object (a named list with class \code{"pg_config"}).
}
\description{
Constructs a configuration object with analysis parameters for PRIO-GRID.
Config objects are session-scoped and not persisted across sessions. Use
\code{\link[=pg_set_rawfolder]{pg_set_rawfolder()}} for persistent machine-level settings.
}
\examples{
cfg <- pg_config(nrow = 180, ncol = 360)
cfg$nrow
}
