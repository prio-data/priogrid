# PGOptionsManager has been removed.
# Use pg_config(), pg_current_config(), pg_set_config() for analysis parameters.
# Use pg_rawfolder(), pg_set_rawfolder() for the persistent raw data folder.
# See R/config.R for the implementation.
