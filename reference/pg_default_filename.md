# Default local filename for a download url

The last path segment of the url, with any query string removed.

## Usage

``` r
pg_default_filename(url)
```

## Arguments

- url:

  Character vector of urls.

## Value

Character vector of filenames.
