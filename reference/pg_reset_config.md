# Reset session configuration to defaults

Resets the current session config to the defaults from
[`pg_config()`](http://prio-data.github.io/priogrid/reference/pg_config.md).

## Usage

``` r
pg_reset_config()
```

## Value

The default `pg_config` object (invisibly).
