# Get the raw data folder path

Returns the persistent rawfolder path. Errors with a helpful message if
unset.

## Usage

``` r
pg_rawfolder()
```

## Value

Character string with the raw data folder path.
