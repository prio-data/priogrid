# Print a pg_config object

Print a pg_config object

## Usage

``` r
# S3 method for class 'pg_config'
print(x, ...)
```

## Arguments

- x:

  A `pg_config` object.

- ...:

  Ignored.

## Value

`x` invisibly.
