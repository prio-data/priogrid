# Get spatial configuration hash

Creates a 6-character MD5 hash of the current spatial options from
config.

## Usage

``` r
get_spatial_hash(config = pg_current_config())
```

## Value

Character string with 6-character hash
